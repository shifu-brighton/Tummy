<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tummy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Tummy" parent="Base.Theme.Tummy" />

    <style name="Theme.Tummy.Fullscreen" parent="Theme.Tummy">

        <item name="android:statusBarColor" tools:targetApi="1">@color/orange</item>
        <item name="android:windowBackground">@color/orange</item>
    </style>

    <style name="ThemeOverlay.Tummy.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/orange</item>
        <item name="fullscreenTextColor">@color/orange</item>
    </style>
</resources>